<h1>Data</h1>

<% @drinks.each do |drink| %>
<p>
Name :<%= drink.name %> <%= link_to 'Edit', edit_drink_path(drink) %> | <%= link_to 'Delete', drink, method: :delete %> <br />
Size :<%= drink.size %>
</p>

<% end %>

<%= link_to 'Add', new_drink_path %>